
// INICIAR SERVER LOCAL
mkdocs serve

// SUBIR CAMBIOS A GITHUB
git add .
git commit -m "Descripción breve de los cambios realizados"
git push -u origin master


// Cada vez que se haga un rebuild

mkdocs build
mv site/* . o rsync -av site/ ./
rmdir site o rm -r site
git add .
git commit -m "Reorganized directory structure"
git push origin master


git add .
git commit -m "Reorganizado la estructura de directorios usando rsync"
git push origin master
